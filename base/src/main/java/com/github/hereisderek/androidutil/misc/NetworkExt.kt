package com.github.hereisderek.androidutil.misc

import android.content.Context
import android.net.NetworkInfo
import com.github.hereisderek.androidutil.misc.Managers.connectivityManager

/*
object NetworkExt {
    fun Context.isNetworkAvailable(): Boolean {
        val state = connectivityManager.activeNetworkInfo.state
        return (state == NetworkInfo.State.CONNECTED || state == NetworkInfo.State.CONNECTING)
    }

    fun Context.getNetworkType(): Int = getConnectivityManager().activeNetworkInfo.type
}*/
