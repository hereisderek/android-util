name: Android CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: check out
        uses: actions/checkout@v1
        with: 
          fetch-depth: 1

      - name: build
        uses: "vgaidarji/android-github-actions/build@v1.0.0"
        run: echo "aaa"
